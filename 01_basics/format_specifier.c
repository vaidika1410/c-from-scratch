#include <stdio.h>

int main()
{
    int a = 10;
    float b = 5.5;
    char c = 'v';

    int d = 45 + 9;

    // printf("value of a is %d", a);
    // printf("\nvalue of b is %f", b);
    // printf("\nvalue of c is %c", c);

    printf("value of a is %d \nvalue of b is %f \nvalue of c is %c", a, b, c);
    printf("\nvalue of d is %d", d);
    printf("\nvalue of d is %d", a+44); 
    return 0;
}